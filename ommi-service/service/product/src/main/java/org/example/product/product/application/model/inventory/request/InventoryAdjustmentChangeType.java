package org.example.product.product.application.model.inventory.request;

public enum InventoryAdjustmentChangeType {
    on_hand,
    available,
    reserved,
    committed,
    incoming,
    cost_price;
}
